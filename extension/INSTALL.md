# 电商助手 · 浏览器插件（演示版 v0.2）

## 安装（Chrome / Edge）

1. 解压本 ZIP 到任意目录（文件夹内需包含 `manifest.json`）。
2. 打开 `chrome://extensions`（Edge：`edge://extensions`）。
3. 开启右上角 **开发者模式**。
4. 点击 **加载已解压的扩展程序**，选择解压后的文件夹。

## 使用流程

1. **登录源站**（如 1688、淘宝）商品详情页。
2. 点击浏览器工具栏中的插件图标 → **采集当前页**。
3. 确认预览后点击 **上传到采集箱**（会打开本后台采集箱并导入）。
4. 在后台 **处理工作台** 编辑标题、价格、翻译等。
5. 在 **发布中心** 创建任务后，打开 Shopee / TikTok / 淘宝卖家后台，用插件 **填入草稿**。
6. 在目标平台卖家中心检查草稿并 **正式发布**。

## 后台地址（可在插件内修改）

默认：`https://tuerqidelvfanzi.github.io/E-comassit-plan`

本地开发：`http://localhost:3004`

## 说明

- 演示版采集箱导入通过打开后台 URL 参数完成，无需 API。
- 1688 详情页采集较完整；其他站点为尽力解析。
- 正式发布需对接后端 API 与平台账号授权。
